# 绪论
## 研究背景和意义
## 国内外研究情况
## 论文的主要工作
## 论文的内容组织
# 项目工程的技术基础
## JavaScript事件循环机制
## Vue.js框架
## TypeScript的类型检查机制
## TailwindCSS框架
## 面向对象设计思想
# 项目工程的需求分析
## 登录注册模块需求分析
## 试验台模块需求分析
## 数据展示模块需求分析
## 国际化与样式模块需求分析
# 项目工程的详细设计
## Madison框架设计
## PromiseHelper设计
## RouterPromise设计
## 数据展示模块设计
### 日志展示
### 指标展示
### 调用链展示
# 项目工程的具体实现
## 前端页面实现
## Madison框架实现
# 测试
## 前端界面测试
## 路由测试
## Madison框架测试
## 后端接口测试
# 总结与展望
## 本文总结
## 进一步展望
# 参考文献
# 致谢


```mermaid
architecture-beta
    service user(mdi:account)
    service lambda(logos:aws-lambda)

    user:R --> L:lambda
```

```mermaid
sequenceDiagram
    DataB->>DataA: 我等待你数据获取完毕
    DataA->>PromiseHelper: 我需要返回给await等待的promise
    PromiseHelper->>DataA: 返回校准Promise
    DataA->>DataB: 等待校准Promise完成
    activate DataB
    Note right of DataB: await promise
    DataA->>PromiseHelper: 我数据获取完了，调用ph.resolve()
    Note left of PromiseHelper: promise.resolve()
    PromiseHelper->>DataB: 校准Promise状态改变，await结束
    deactivate DataB
    DataB->>DataA: 我需要你的数据
    DataA->>DataB: 返回数据
```

```mermaid
flowchart LR
    start1([router.beforeEach])
    end1([router.beforeEach完成，next调用])
    precheck{precheck是否需要修改路由}
    check{check是否需要修改路由}
    precheckdoing[运行所有precheck方法]
    checkdoing[运行所有check方法]

    start1 --> precheckdoing
    precheckdoing --> precheck
    precheck -- 是 --> start1
    precheck -- 否 --> checkdoing
    checkdoing --> check
    check -- 是 --> start1
    check -- 否 --> end1
```

```mermaid
flowchart LR
    start2([router.afterEach])
    end2([router.afterEach完成])
    postcheckdoing[运行所有postcheck方法]

    start2 --> postcheckdoing
    postcheckdoing --> end2
```

```mermaid
sequenceDiagram
    actor User
    participant Router
    participant RouterPromise
    participant Data

    Note right of Data: 初始<br/>__key.value=0
    Data->>User: data=data0
    User->>Router: goto https://xxx.cn/xxx?key=1
    Router->>RouterPromise: router.beforeEach
    RouterPromise->>Data: precheck
    Note right of Data: __queryKey.value=1
    Data->>RouterPromise: Done
    RouterPromise->>Data: check
    Note right of Data: search data...
    Data->>RouterPromise: Done
    RouterPromise->>Router: Done
    Note left of Router: 路由切换
    Router->>RouterPromise: router.afterEach
    RouterPromise->>Data: postcheck
    Note right of Data: __key.value=1
    Data->>RouterPromise: Done
    RouterPromise->>Router: Done
    Router->>User: https://xxx.cn/xxx?key=1
    Data->>User: data=data1
```

```mermaid
classDiagram
class MadisonAddon{
    <<Abstract>>
    # readonly Madison __madison
    + abstract logoutCallback() void
}
class MadisonAddonDataBase~DATA~{
    <<Abstract>>
    # Ref~number~ __startTime
    # Ref~number~ __endTime
    # Ref~boolean~ __searching
    # abstract Ref~number~ __apiUseStartTime
    # abstract Ref~number~ __apiUseEndTime
    + abstract get data() ComputedRef~DATA[]~
    + abstract precheck(to: RouteLocationNormalized, from: RouteLocationNormalized) RouterPromiseSyncFuncRes
    + abstract check(to: RouteLocationNormalized, from: RouteLocationNormalized) Promise~RouteLocationRaw | void~
    + abstract postcheck(to: RouteLocationNormalized, from: RouteLocationNormalized) RouterPromiseSyncFuncRes
}

class MadisonAddonDataS2E~DATA~{
    <<Abstract>>
    # WritableComputedRef~number~ __queryStartTime
    # WritableComputedRef~number~ __queryEndTime
    + get startTime() WritableComputedRef~number~
    + get endTime() WritableComputedRef~number~
    # checkSTANDET(to: RouteLocationNormalized) true | RouterPromiseSyncFuncRes
}

class MadisonAddonDataTMR2T~DATA~{
    <<Abstract>>
    # [number, string][] stepTimeList
    # Ref~number~ __selectedRangeIndex
    # Ref~number~ __timestamp
    # Ref~string~ __inputRangeStr
    + get rangeStr() WritableComputedRef~string~
    + get rangeIndex() WritableComputedRef~number~
    + get timestamp() WritableComputedRef~Date~
    # debouncedChangeRangeDataTimestamp(value: number | string)
    # debouncedChangeRangeDataRangeIndex(value: number | string)
    # checkTSANDRI(to: RouteLocationNormalized) true | RouterPromiseSyncFuncRes
}

MadisonAddon <|-- MadisonAddonDataBase
MadisonAddonDataBase <|-- MadisonAddonDataS2E
MadisonAddonDataBase <|-- MadisonAddonDataTMR2T
```

```mermaid
flowchart TD
    start([函数调用])
    getSE[获取startTime和endTime]
    firstInCheck{第一次进入}
    return([return true])
    isNumberCheck{startTime和endTime是否是数字}
    changeToNumber[转换为数字]
    notNumber([重定向到to.name并将
    startTime赋值为this.__startTime.value
    endTime赋值为this.__endTime.value
    ])
    stCheck{startTime是否大于此时或者小于等于0}
    etCheck{endTime是否大于此时或者小于等于0}
    rangeCheck{时间区间是否大于允许的最大时间区间或者小于0}
    stChange([重定向到to.name并将startTime赋值为前一分钟])
    etChange([重定向到to.name并将endTime赋值为现在])
    stetChange([重定向到to.name并将startTime赋值为前一分钟，endTime赋值为现在])
    yes[将startTime赋值给this.__queryStartTime.value,将endTime赋值给this.__queryEndTime.value]

    start --> getSE
    getSE --> firstInCheck
    firstInCheck -- 是 --> return
    firstInCheck -- 否 --> isNumberCheck
    isNumberCheck -- 是 --> changeToNumber
    isNumberCheck -- 否 --> notNumber
    changeToNumber --> stCheck
    stCheck -- 是 --> stChange
    stCheck -- 否 --> etCheck
    etCheck -- 是 --> etChange
    etCheck -- 否 --> rangeCheck
    rangeCheck -- 是 --> stetChange
    rangeCheck -- 否 --> yes
    yes --> return
```

```mermaid
sequenceDiagram
    actor User
    box 指标（总体）展示模块
    participant checkNodeTopology
    participant checkNodeList
    end
    box 其他模块
    participant testbed as 试验台模块
    end

    Note right of User: 进入页面
    activate checkNodeList
    checkNodeTopology ->> testbed: 校验namespace
    testbed ->> checkNodeTopology: 校验通过
    Note left of checkNodeList: 获取nodeList
    checkNodeTopology ->> checkNodeList: 等待nodeList获取
    checkNodeList ->> checkNodeTopology: nodeList获取完毕
    deactivate checkNodeList
    activate checkNodeTopology
    Note right of checkNodeTopology: 构建node拓扑
    checkNodeTopology ->> User: 显示node拓扑
    deactivate checkNodeTopology
    User ->> checkNodeTopology: 选择某一个机器实例，查看详细指标
    Note left of checkNodeTopology: 前往指标（详细）展示模块
```

```mermaid
sequenceDiagram
    actor User
    box 指标（详细）展示模块
    participant checkMetricName
    participant checkNodeList
    end
    box 其他模块
    participant testbed as 试验台模块
    end

    Note right of User: 进入页面（无查询数据）
    checkMetricName ->> testbed: 校验namespace
    testbed ->> checkMetricName: 校验通过
    activate checkMetricName
    Note right of checkMetricName: 获取metricName
    checkNodeList ->> checkMetricName: 等待metricName获取，同时间接校验namespace
    par checkMetricName to checkNodeList
        checkMetricName->>checkNodeList: metricName获取完成，namespace校验完成
    end
    deactivate checkMetricName
    activate checkNodeList
    Note left of checkNodeList: 查询nodeList
    deactivate checkNodeList
    checkNodeList ->> User: nodeList和metricName获取完毕，渲染页面
```

```mermaid
sequenceDiagram
    actor User
    box 指标（详细）展示模块
    participant checkMetricName
    participant getMetricData
    end
    box 其他模块
    participant testbed as 试验台模块
    end

    Note right of User: 进入页面（有查询数据）
    checkMetricName ->> testbed: 校验namespace
    testbed ->> checkMetricName: 校验通过
    activate checkMetricName
    Note right of checkMetricName: 获取metricName（如果没获取）
    par checkMetricName to getMetricData
        checkMetricName->>getMetricData: metricName获取完成，namespace校验完成
    end
    deactivate checkMetricName
    getMetricData ->> checkMetricName: 询问查询的metricName是否合法
    checkMetricName ->> getMetricData:返回合法结果
    activate getMetricData
    Note left of getMetricData: 查询没有的metricData
    getMetricData ->> User: metricData获取完毕，渲染页面
    deactivate getMetricData
```

```mermaid
sequenceDiagram
    actor User

    create participant LoongCalendar
    User->>LoongCalendar: useCalendar id=test
    create participant VueCalendar
    User->>VueCalendar: 使用日历组件 id=test
    Note right of LoongCalendar: Start rendering Canvas
    VueCalendar->>LoongCalendar: useCalendar id=test
    User->>LoongCalendar: addSchedule
    LoongCalendar->>VueCalendar: render Canvas
    User->>LoongCalendar: removeSchedule
    LoongCalendar->>VueCalendar: render Canvas
    destroy VueCalendar
    User->>VueCalendar: 销毁日历组件
    Note right of LoongCalendar: Stop rendering Canvas
    destroy LoongCalendar
    User->>LoongCalendar: destoryCalendar id=test

```
